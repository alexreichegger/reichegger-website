# Postgres connection string. Local dev: a local Postgres 16.
# Production: the Supabase pooled connection string.
DATABASE_URL="postgresql://coach:coach@localhost:5432/coach"

# Coach chat (claude-sonnet-5, server-side only). Without it the app runs
# fine but the chat replies with an offline notice.
ANTHROPIC_API_KEY=""
